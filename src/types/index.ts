export interface TrackInfo {
  url: string;
  artist: string;
  title: string;
  duration: number;
  artwork_url: string | null;
  id: number;
  playback_count: number | null;
  likes_count: number | null;
}

